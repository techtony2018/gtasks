# Mission Control V0.0.155 release evidence

## Released baseline

- Version: `V0.0.155`
- Date: `2026-08-24` (`America/Los_Angeles`)
- Commit: `05dce491ab298a5c11c05b791e2f4c0a683de4f6`
- Title: `Surface stale queued Goal handoffs`
- Dashboard-managed health readback: `http://127.0.0.1:4179/api/health`
  returned `V0.0.155`.
- Test evidence: full suite `1348` OK, skipped `5`.
- Independent QA PASS:
  `artifacts/qa/v0.0.155-independent/gate-report.md`.

## Verified behavior

Goal execution now surfaces `handoff_worker_unavailable` when a Goal-derived
active task has latest handoff status `queued` and the dispatcher execution
claim remains nonterminal after the bounded worker attention window. The UI
renders Needs attention with exact copy:

`The canonical task is active and queued, but no verified Agent worker has
leased it yet. Verify the Agent host dispatcher and private route.`

Fresh queued handoffs remain Delivering. `handoff_missing`,
`handoff_needs_repair`, `task_needs_next_action`, ordinary `duplicate`, and
`recently_completed` remain distinct.

## Operational readback

Toddy Health Goal `goals/d175890b-6e89-5543-b587-b5df345c1c81` currently
surfaces `handoff_worker_unavailable` for active task
`tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`; the latest handoff status is
`queued`.

The operational diagnosis is that Toddy fixed thread/host configuration exists,
but the Toddy host's Tailscale session is logged out. The private route to
`https://tonys-macbook-pro.taildb46a7.ts.net` is unreachable, so no worker
lease or delivery can complete until the host route is repaired.

## Documentation boundary

This evidence is an operational route/host remediation item. It is not a
completed Agent execution, not a completed task claim, and not evidence of a
verified worker lease. Documentation must not mutate task lifecycle, fabricate
handoff delivery, or change Agent work state to match this projection.
