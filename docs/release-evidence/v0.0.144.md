# Mission Control V0.0.144 release evidence

## Released baseline

- Version: `V0.0.144`
- Date: `2026-08-24` (`America/Los_Angeles`)
- Commit: `c96a68464b2046e391924eb5a41a1f63aeafe687`
- Included handoff recovery release commits:
  - `001d742` — V0.0.141, stale local `abandon_start` rows reconcile against
    authoritative `/recover` completed/suppressed server state.
  - `4608077` — V0.0.142, `codex_thread_active_writer` is retryable local
    Codex backpressure, not terminal delivery.
  - `3cd682c` — V0.0.143, operator recovery may requeue an owned
    `terminal_delivery_failure` only when abandoned execution starts prove the
    failed starts were unused.
  - `c96a684` — V0.0.144, active-writer retries use bounded 300-second local
    backoff.
- Dashboard-managed health readback:
  `http://127.0.0.1:4179/api/health` returned `V0.0.144`.
- Developer release handoff reported full suite `1336` OK with `5` skipped;
  focused handoff and local-dispatcher suites passed.

## Verified behavior

Finance stale handoff `handoff-3369...` read back as completed. Direct server
handoff store readback for
`handoff-3369d650b7dceac56adbd05a8e2912b0127e18fdc0bd54170ae351dd08d9ba16`
showed status `completed`, task
`tasks/3d54d11c-db8e-59bf-8039-e050fa763dc9`, and detail
`Completed Finance progress review and verified internal Agent Artifact publication`.
Its audit events ended with `acknowledgement` status `completed` and
`execution_claim_released` status `completed`.

Career handoff `handoff-806...` read back as recovered retryable work. Direct
server handoff store readback for
`handoff-806a9277e5e24fda408872671512b27a4f9285494cfabc6f6de61a421dddac9f`
showed status `received`, reason `system_dependency_recovered`, task
`tasks/a6251324-1af6-5005-8a17-0ad0610be4d8`, and attempt `2`. Its audit
events show the previous `dead_letter`/`terminal_delivery_failure`, the
operator `delivery_retry`, and subsequent abandoned unused starts; after
V0.0.144, active-writer attempts should back off instead of immediately
dead-lettering. Developer handoff reported a live local retry gate with
`retry_at` `2026-08-24T07:21:55Z`.

## Boundary

This release documents recovery behavior only. It does not change the
standing deployment policy: current behavior must be committed, pushed,
dashboard-deployed, and health/version read back before documentation calls it
current. It does not authorize documenting host-private token values,
credentials, raw prompts, or private thread identifiers.
