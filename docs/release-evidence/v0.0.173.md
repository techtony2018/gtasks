# Mission Control V0.0.173 — Board range presets and Goal dedup contracts

Date: 2026-08-25

## Scope

- Tickets:
  - `tasks/e313548c-7a26-401d-820f-eeb34789d29d`
  - `tasks/c930682a-abe1-432f-b267-d09bf024ac52`
- Board view now defaults to `3 Days Before and After Today`.
- Board view now includes exact presets `3 Days Before and After Today` and `One Week Before and After Today`, while retaining two-week, thirty-day, and all-date options.
- Goal read contracts now explicitly deduplicate repeated root backlinks by exact slug while preserving distinct canonical Goal pages even when titles match.

## Verification

- `node --check static/app.js`
- `python3 -m unittest tests.test_frontend_contract.FrontendContractTests.test_board_default_window_is_three_days_with_week_preset_and_keeps_actionable_undated_tasks tests.test_gbrain.GoalReadTests.test_goal_read_deduplicates_repeated_root_backlinks_by_slug tests.test_gbrain.GoalReadTests.test_ignores_goal_like_concepts_that_only_mention_goal_root`
- Full regression and independent QA pending before commit.

## Notes

- The Board range remains a session-scoped UI preference and does not mutate canonical GBrain tasks.
- Active, blocked, and undated tasks remain visible by default.
- Goal deduplication remains canonical slug based; the UI does not infer merges from title text alone.
