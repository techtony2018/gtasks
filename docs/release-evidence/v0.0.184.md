# V0.0.184 — Expose Goal execution blocking question

## Scope

- `/api/goal-execution.summary` and `last_run.summary` now include
  `blocking_questions` for `waiting_for_tony` decisions when the exact
  canonical question TODO can be read.
- Agents > Goal execution visibly shows the current blocking question with a
  `Question:` prefix:
  `Question: Which family-care scope, outcomes, constraints, and first action should Toddy use next?`
- The blocking question is readback/display context only. It does not answer
  Tony's question, remove the blocker, acknowledge the Agent handoff, wake
  Toddy, or complete the Agent task.

## Canonical blocking question

- Goal: `goals/2c86f86c-c9fb-5f49-96d0-e4d63f489fc8`
- Task: `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
- TODO: `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`
- Agent: `agents/toddy`
- Question: `Which family-care scope, outcomes, constraints, and first action should Toddy use next?`
- Detail: `Based on Artifact artifacts/0e0323e7-f5b8-4833-881a-018507ac7e2a, please decide: scope categories accepted or revised; desired outcomes accepted or revised; constraints accepted or revised; and proposed first action approved, revised, or declined. Until Tony answers, no further family-related inspection, outreach, scheduling, purchasing, or action is authorized.`

## Verification

- Focused check: 3 tests OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK, 5 skipped.

## QA

- Independent desktop 1440x1000 and genuine mobile 390x844 QA passed for the
  unchanged eight-file candidate with aggregate
  `c2f7c7769335285fa4dd5ac6f1b19cb7d58d6767b9d1e4a8279a2e39bc14c1b8`.
- QA report: `artifacts/qa/v0.0.184-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.184-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.184.
- Live `/api/goal-execution` top-level summary matched `last_run.summary` and
  included exactly one `blocking_questions` entry for the canonical
  Family/Toddy question above.
- Postdeploy `/api/goal-execution` readback had `last_error=null` and summary
  counts still at:
  - `total_goals=7`
  - `needs_attention=2`
  - `waiting_for_tony=1`
  - `owner_missing=1`
  - `in_flight=1`
  - `recently_completed=3`
- Desktop and mobile both visibly rendered the exact `Question:` copy, while
  the V0.0.183 next action and all summary counts remained visible.
- QA confirmed six Agent cards and seven Goal cards stayed visible and
  contained; the canonical Family/Toddy Task opened populated detail and
  restored origin at both widths; mobile detail stayed x0/w390 with focus
  trapped; local worker configs remained exactly `agents/tammy` and
  `agents/tammy-oc`; console warnings, request failures, non-GET browser
  methods, and GBrain mutations were all zero.
- Landed commit:
  `525e23119c4484f97c9bd816ea8b0af03729d3ac`
  (`Expose goal execution blocking question`).
