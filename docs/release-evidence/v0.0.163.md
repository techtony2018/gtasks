# Mission Control V0.0.163 release evidence

Date: 2026-08-24 (`America/Los_Angeles`)

Release: `V0.0.163`

Title: Expose Goal handoff questions

Runtime target: dashboard-managed Mission Control at `http://127.0.0.1:4179`.

## Change

Goal execution rows and Agent cards now surface the exact open question TODO for
Goal-derived work that is blocked waiting for Tony. The UI also merges richer
Agent-work projections with same-slug snapshot rows so handoff and TODO context
is not hidden by a thinner cached task row.

The current Family/Toddy blocker now renders:

`Answer: Which family-care scope, outcomes, constraints, and first action should Toddy use next?`

This keeps the blocked state honest while making the next required action
visible directly from the Agents Goal execution surface.

## Verification

- Focused/static checks:
  `python3 -m unittest tests.test_frontend_contract tests.test_releases`
  reported `263` tests OK.
- Full suite:
  `python3 -m unittest discover -s tests` reported `1360` tests OK,
  `5` skipped.
- `node --check static/app.js` passed.
- `python3 -m compileall -q gtasks tests` passed.
- Independent pre-commit QA PASS:
  `artifacts/qa/v0.0.163-independent/gate-report.md`.
- Frozen QA aggregate:
  `2544cd274a848a35eaf3dd080c8a1814f1730da6e229dcc65074cab3ca657539`.

Independent QA verified desktop 1440x1000 and genuine mobile 390x844:

- Family/Toddy Goal execution row and Toddy compact Agent card both show the
  exact question text.
- The exact Family task link opens
  `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`, focuses the detail title, and
  restores exact origin on Close.
- Mobile full-sheet modal, inert background, focus trap, and 390px containment
  pass.
- Source-blind same-slug thin snapshot plus richer Agent-work fixture preserves
  handoff/todos.
- Career and Finance auto-canary completed readbacks remain visible and their
  exact Artifacts still read back as `produced_for` the completed Tasks.
- Browser flow stayed GET-only with no live GBrain or Buzz mutation.

## Current live readbacks

- Family/Toddy task:
  `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
- Question TODO:
  `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`
- Finance/Tammy completed task:
  `tasks/cc655813-1968-5264-a5ad-454199c1b3cb`
- Finance Artifact:
  `artifacts/9362d402-0f7c-4d65-9222-a8c140f1d9d3`
- Career/Tammy completed task:
  `tasks/53264f17-e5d5-5b5d-ad36-af1eadc1a770`
- Career Artifact:
  `artifacts/fbffd8c1-b04e-420f-8db3-14be7a2b7f8f`
