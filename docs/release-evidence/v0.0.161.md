# Mission Control V0.0.161 release evidence

Date: 2026-08-24 (`America/Los_Angeles`)

Release: `V0.0.161`

Commit: `28a9c8de5f3191f261c91adfa04d156f0667c5c6` (`Surface active Goal handoff status`)

Runtime: dashboard-managed Mission Control at `http://127.0.0.1:4179`; `/api/health` read back `V0.0.161`.

QA and tests:

- Independent QA PASS:
  `artifacts/qa/v0.0.161-independent/gate-report.md`
- Frozen QA aggregate:
  `8b4d97e103e48ea4b61dfc2d2787f1a84ab16ced2cadbd2a740319a7f920e5fd`
- Full suite: `python3 -m unittest discover -s tests` reported `1357` OK,
  `5` skipped.
- Static/runtime checks reported by release handoff:
  `node --check static/app.js` and
  `python3 -m compileall -q gtasks tests` passed.

Verified behavior:

- Goal execution retains accepted dispatcher handoff status for selected
  duplicate or recent active Goal tasks, so the UI can render Delivering or
  Executing instead of ambiguous Ready or duplicate-only context.
- Independent QA verified an active Faith/Tammy duplicate fixture with
  `handoff.status=actively_executing` rendered `Executing` on desktop
  1440x1000 and genuine mobile 390x844.
- This is readback/presentation context only. It does not mean all Goals are
  automated, does not expand the configured canary boundary, and does not
  satisfy completion without terminal handoff plus exact Artifact evidence.

Live operational readback:

- Runtime canary target rotated from Toddy Health to Faith/Tammy.
- Faith/Tammy task:
  `tasks/46ba34c2-9ccb-523e-a786-9b70d5673073`
- Task status: `completed`
- Owner Agent: `agents/tammy`
- Faith Goal: `goals/755548a3-d556-513a-900c-45f90da5702e`
- Artifact:
  `artifacts/d2a45c21-1428-4891-ae98-531a958e1e98`
- Artifact provenance: `created_by=agents/tammy`,
  `produced_for=tasks/46ba34c2-9ccb-523e-a786-9b70d5673073`, and
  `supports_goal=goals/755548a3-d556-513a-900c-45f90da5702e`.
- `/api/goal-execution` read back Faith as `recently_completed` for the
  completed task.

Remaining next-owner blockers:

- Family/Toddy task
  `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d` remains legitimately
  `blocked`, with canonical handoff `waiting_for_input`,
  `waiting_on=people/tony-guan`, and question TODO
  `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`.
- Toddy Health task
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5` remains active and
  infrastructure-blocked with dispatcher status `queued` /
  `handoff_worker_unavailable` until the Toddy fixed-thread host is logged into
  Tailscale and its dispatcher route is available.
