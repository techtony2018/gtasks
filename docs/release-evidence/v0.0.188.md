# V0.0.188 — Classify Goal execution action ownership

## Scope

- `/api/goal-execution.summary` and `last_run.summary` now include structured
  `action_queue` entries that group next actions by owner.
- Queue entries include `owner`, `kind`, `label`, relevant Goal/Task/TODO/Agent
  slugs, and a bounded `summary`.
- Agents > Goal execution renders the queue with:
  - `Action queue:`;
  - `Tony action required`;
  - `Answer Agent question`;
  - `Assign Goal owner`.
- Existing V0.0.187 explicit assignment controls remain: `Assign to Tammy`,
  `Assign to Timmy`, and `Assign to Toddy`.
- Existing V0.0.186 exact Task and Goal action links still restore focus to the
  exact summary origin.

## Canonical action queue

- Owner: `tony`
  - Kind: `answer_question`
  - Label: `Answer Agent question`
  - Task: `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
  - TODO: `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`
  - Agent: `agents/toddy`
- Owner: `tony`
  - Kind: `assign_goal_owner`
  - Label: `Assign Goal owner`
  - Goal: `goals/d837ac94-36f5-4735-93bb-d84c69b45435`
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- Postdeploy public reason: `actively_executing`

## Verification

- Focused checks: 238 OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Independent desktop and genuine mobile QA passed for the unchanged six-file
  product candidate with aggregate
  `7be6883d45ada59030d0db7c9509244a339828f96cca1a7ee32a005196958697`.
- QA report: `artifacts/qa/v0.0.188-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.188-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.188.
- Postdeploy `/api/goal-execution` readback was terminal:
  - `last_run` present;
  - `public_reason=actively_executing`;
  - selected task `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`;
  - `summary.action_queue` length `2`;
  - `last_error=null`.
- `/api/goal-execution.summary.action_queue` contained exactly the two
  Tony-owned actions required by the candidate:
  - `answer_question` / `Answer Agent question` for task
    `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`, TODO
    `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`, Agent `agents/toddy`;
  - `assign_goal_owner` / `Assign Goal owner` for Entrepreneurship Goal
    `goals/d837ac94-36f5-4735-93bb-d84c69b45435`.
- Desktop and mobile visibly showed `Action queue:`,
  `Tony action required`, `Answer Agent question`, and `Assign Goal owner`
  while retaining the counts, next action, Family question, and
  Entrepreneurship/default_agent_for evidence.
- Existing controls remained exact:
  - `Assign to Tammy`, `Assign to Timmy`, and `Assign to Toddy` rendered;
  - no OpenClaw assignment control appeared;
  - the isolated Tammy fixture still captured
    `POST /api/agents/agents%2Ftammy/default-goals` with body
    `{"goal_slug":"goals/d837ac94-36f5-4735-93bb-d84c69b45435","action":"assign"}`
    before live network and did not mutate live GBrain;
  - the Family question and Entrepreneurship links opened their canonical
    details and restored focus to the exact summary origin after 1900ms.
- Fresh live desktop/mobile sessions had zero console warnings/errors, zero
  failed requests, zero HTTP responses at or above 400, and only GET app
  methods. The only POST was the separately intercepted in-memory assignment
  fixture.
- Landed commit:
  `7484f86ae95fd0dbc496f0e41f9b4376397af202`
  (`Classify goal execution action ownership`).
