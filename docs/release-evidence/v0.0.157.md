# Mission Control V0.0.157 release evidence

## Released baseline

- Version: `V0.0.157`
- Date: `2026-08-24` (`America/Los_Angeles`)
- Commit: `5b9dbb1cdfe372e1a3b067230749172ba30ab193`
- Title: `Cycle Goal-derived Agent reviews`
- Dashboard-managed health readback: `http://127.0.0.1:4179/api/health`
  returned `V0.0.157`.
- Independent QA PASS:
  `artifacts/qa/v0.0.157-independent/gate-report.md`.
- Frozen QA aggregate:
  `dc485a26f8439327c4276b5b0ca429010291d6e6d5053dbff4f3c6f63ace94cd`.
- Focused QA suite evidence: `python3 -m unittest tests.test_goal_execution`
  reported `39` passed.

## Verified behavior

Goal-derived Agent review Tasks now include a cycle key in the deterministic
fingerprint. A completed prior-cycle review no longer permanently suppresses
the next bounded review cycle, preventing Goals from getting stuck at
`recently_completed` forever. Same-cycle completed work still suppresses
repeat creation.

Independent QA verified different deterministic Task slugs for review cycles:

- cycle `2026-08-17`: `tasks/5a7ef64f-504f-5cb0-80d3-eab0728b3801`
- cycle `2026-08-24`: `tasks/44e14ea5-0f81-558b-a761-ec3540f3b4e2`

The cycle change does not weaken the V0.0.156 completion rule: completed
handoff without exact Artifact `produced_for` readback remains active/duplicate.

## Operational readback

After deploy, the dashboard-managed scheduler created and activated
current-cycle Civic/Timmy task
`tasks/44e14ea5-0f81-558b-a761-ec3540f3b4e2`, owned by `agents/timmy`, for Goal
`goals/41fb50e0-e1d7-592b-b2c3-ff1f7aacff10`. The task detail includes
`Review cycle starts 2026-08-24`. Developer handoff reported the dispatcher
progression from leased to execution_started to actively_executing. Later
documentation readback showed the task still active with projected
`dispatcher_handoff.status=suppressed`. No Artifact was present at the bounded
handoff check.

The canary target was restored to Toddy Health afterward. Toddy still needs the
previously reported Tailscale host login repair; its queued handoff remains
`handoff_worker_unavailable` until the private route is restored.

## Documentation boundary

This release permits the next bounded review cycle after prior-cycle
completion. It is not unbounded recurring execution, does not bypass WIP or
identity checks, and does not complete an active task without handoff plus
Artifact evidence. Same-cycle completed work still suppresses repeats.
