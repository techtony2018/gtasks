# V0.0.67 release evidence

## Scope

- Agent information requests remain normal canonical tasks and use `blocked`,
  one typed Tony blocker, one canonical question To Do, and one structured
  handoff projection. Mission Control does not create or restore a `waiting`
  task status.
- **Answer and Hand Back** records the immutable answer, completes the question,
  removes only the matching Tony blocker, preserves unrelated blockers, and
  returns the same task to its assigned Agent with a precise resume action and
  exact GBrain readback. Agent acknowledgement and bounded follow-up questions
  continue on that same task.
- The Calendar picker is compact, remains open while saving, verifies the saved
  calendar identifiers before closing, and reports persistent success or an
  actionable retained error. The branded read-only EventKit helper accepts the
  actual LaunchServices argument shape and performs no calendar writes.

## Independent UI/UX gate

- The first frozen candidate failed because serialized Agent slugs rendered as
  lowercase display names in handoff copy. No commit was made from that failed
  candidate.
- The repaired frozen candidate passed independent desktop `1440x1000` and
  genuine mobile `390x844` QA before commit.
- QA verified the exact Tammy handoff copy, question/answer reconciliation,
  focus, conflict preservation, read-only history, non-handoff Blocked copy,
  ordinary To Dos, compact Calendar picker layout, Saving state, verified
  readback success, retained failure, live Calendar containment/default-on
  behavior, task surfaces, About/version, status options, and zero live writes.
- Full report:
  `artifacts/qa/v0.0.67-retest-independent/gate-report.md`.

## Developer verification

- `python3 -m unittest discover -s tests` — 350 passed.
- `node --check static/app.js` — passed.
- `python3 -m py_compile gtasks/*.py` — passed.
- `git diff --check` — passed.
- The signed branded helper was rebuilt at
  `/Users/tony/Library/Application Support/Mission Control/Mission Control Calendar.app`;
  its executable SHA-256 is
  `13050c9fdbc7d2af6d65915f887fdcfe0b6157988396ee73d3f48cfe7f9d6321`.
- A live read-only event request for `2026-07-27` through `2026-08-02`
  returned HTTP 200 with the current `not_determined` permission state and no
  events; the earlier invalid-helper-response 503 is removed. Mission Control
  did not request Calendar permission during verification.

## Deployment and fixed-task protocol readback

- Commit `c2766608aac5dd686e76998bb90845082c1d022e` was pushed to
  `origin/main` before the dashboard-managed `gtasks` service was restarted.
  `http://127.0.0.1:4179/api/health` then reported `V0.0.67`.
- Tammy's existing fixed task remains
  `019fb4e7-8846-71a0-8d4b-24d262979981`; Timmy's remains
  `019fb4e5-37bd-7d01-801b-2d5ba9c70611`; Toddy's remains
  `019fb4df-1618-7b12-89ab-df210a5912cd`. No new Codex task was created.
- All three daytime protocols now inspect the oldest unacknowledged
  `handoff.state=ready_for_agent` before normal task selection, acknowledge it,
  continue the bounded resume action, and keep any follow-up question on the
  same canonical task. Readback shows hourly `09:00` through `19:00`
  America/Los_Angeles schedules.
- The shared installed runner SHA-256 on Tammy, Timmy, and Toddy is
  `97950f9fc331dda5cff247668c59002e5917744faeb88ce97431b5884c652275`;
  the shared hourly work plist SHA-256 is
  `4af4e122c12d303697426604777443b96b1665e0be135c3030d204d70ad23b91`.
- Empty stale Agent lock directories dated July 31 were removed only after
  process inspection confirmed that no corresponding steward runner was
  active; each LaunchAgent was reloaded and read back as installed.

## Authorized live-task disposition

- Before the authorized compatibility repair could write anything,
  `tasks/415adeea-1d91-4df4-8c48-4c15c0334a3b` read back as already
  `completed` by Tammy at `2026-08-02T11:06:53.684145-07:00`, with the full
  seven-day Bible-study plan, its original recorded answer preserved, no
  blockers, and a new Tony-owned next action.
- The released repair path failed closed with
  `only authorized unfinished Agent work can be repaired` before creating any
  comment, event, page, or relationship. It did not regress the completed task
  to `active` or overwrite Tammy's result.
- `/api/agent-work?refresh=1` then returned the completed task once, owned by
  `agents/tammy`, in `collections/tammys-tasks`, linked to
  `goals/755548a3-d556-513a-900c-45f90da5702e`, with zero task issues. The
  preserved answer To Do remains Done, and the canonical open next action is
  `Tony starts Day 1 with Psalm 1:1–6 and records the date, passage, and
  one-sentence reflection.`
- Durable diagnostic output is stored at
  `/tmp/gtasks-v0.0.67-live-handoff-repair.log` with exit status at
  `/tmp/gtasks-v0.0.67-live-handoff-repair.exit`.
