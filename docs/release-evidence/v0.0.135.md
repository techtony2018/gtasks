# Mission Control V0.0.135 release evidence

## Released baseline

- Version: `V0.0.135`
- Date: `2026-08-23` (`America/Los_Angeles`)
- Release commits:
  `e3640f716b4cc6ab1c7ac0ab5613402cbcfc20a4` and
  `9430533a9e97b3fc436043be0d48db9b6f6a6855`
- Dashboard-managed health during independent QA:
  `http://127.0.0.1:4179/api/health` returned `V0.0.135`
- Developer handoff reported full suite result:
  `python3 -m unittest discover -s tests` -> `1330` tests OK, `5` skipped
- Independent QA:
  `artifacts/qa/v0.0.135-independent/gate-report.md`
  and `artifacts/qa/v0.0.135-independent/gate-results.json`

## Verified behavior

V0.0.135 combines the controlled Codex-only Goal execution canary and the stale
dispatcher-recovery suppression for completed task details.

The verified completed Goal-derived task is
`tasks/83ed4e35-46a2-5a40-b3a3-502c573c7dea`. Direct canonical readback showed
status `completed`, `member_of -> collections/tammys-tasks`,
`assigned_to -> agents/tammy`, and
`advances_goal -> goals/755548a3-d556-513a-900c-45f90da5702e`.

The task retained exactly one canonical Artifact:
`artifacts/5f35baf9-e7fb-44f4-a28a-cd88e8e9581c`, titled
`Faith Bible Study progress brief — 2026-08-23`, with typed
`member_of -> collections/tammys-artifacts`,
`created_by -> agents/tammy`, and
`produced_for -> tasks/83ed4e35-46a2-5a40-b3a3-502c573c7dea`.

The V0.0.135 independent QA also verified that completed task detail no longer
renders false `Verified Agent handoff needs system review` recovery attention,
while an active synthetic suppressed dispatcher state still renders repair
attention. The synthetic fixture recorded zero external mutations and no
GBrain, NATS, OpenClaw, or wake connections.

The local Tammy supervisor LaunchAgent persisted
`--codex-resume-timeout 1800`. Private Goal execution runtime configuration was
returned to `shadow` after the controlled Faith canary.

## Documentation boundary

During the documentation follow-up, the working tree contained uncommitted
product changes for a later `V0.0.136` candidate in `gtasks/goal_execution.py`,
`gtasks/releases.json`, and `tests/test_goal_execution.py`, and the managed
health endpoint reported `V0.0.136`. Those changes were not part of the pushed
V0.0.135 evidence above, so this evidence file does not document V0.0.136 as a
verified released baseline.

This note is historical. V0.0.136 and V0.0.137 were later pushed and verified;
see `docs/release-evidence/v0.0.136.md` and
`docs/release-evidence/v0.0.137.md` for the superseding release evidence.
