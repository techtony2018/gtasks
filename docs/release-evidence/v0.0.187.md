# V0.0.187 — Assign missing Goal owners from summary

## Scope

- Agents > Goal execution missing-owner action items now expose explicit
  Codex-only owner assignment controls:
  - `Assign to Tammy`;
  - `Assign to Timmy`;
  - `Assign to Toddy`.
- Each control uses the verified
  `POST /api/agents/<agent>/default-goals` contract with body
  `{goal_slug, action: "assign"}` only after explicit user activation.
- No owner is assigned automatically, inferred from the summary, or repaired in
  the background.
- No OpenClaw assignment controls are exposed.
- Existing V0.0.186 action links remain: the question Task link and
  missing-owner Goal link still restore focus to the exact summary origin.

## Canonical action items

- Missing-owner Goal: `goals/d837ac94-36f5-4735-93bb-d84c69b45435`
- Missing-owner relationship: `default_agent_for`
- Blocking question Task: `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- Postdeploy public reason: `actively_executing`

## Verification

- Focused frontend contract: OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Independent desktop and genuine mobile QA passed for the unchanged
  four-file product candidate with aggregate
  `ae715146d8fc04662fd54ded5fc85c089172ae58684c121fcea5b6f65eabc09d`.
- QA report: `artifacts/qa/v0.0.187-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.187-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.187.
- Live `/api/goal-execution` readback had `last_error=null`,
  `public_reason=actively_executing`, selected task
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`, and counts
  `total_goals=7`, `needs_attention=2`, `waiting_for_tony=1`,
  `owner_missing=1`, `in_flight=1`, `recently_completed=3`.
- The exact Entrepreneurship/default_agent_for missing-owner record remained:
  `goals/d837ac94-36f5-4735-93bb-d84c69b45435`.
- The exact Family/Toddy blocker remained present:
  task `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`, TODO
  `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`, Agent `agents/toddy`.
- Desktop and mobile both showed exactly `Assign to Tammy`, `Assign to Timmy`,
  and `Assign to Toddy` beside the Entrepreneurship missing-owner evidence.
- QA found zero OpenClaw assignment controls or OpenClaw assignment copy.
- In an isolated browser fixture, QA intercepted the assignment request before
  network. Activating `Assign to Tammy` generated exactly:
  - endpoint `/api/agents/agents%2Ftammy/default-goals`;
  - method `POST`;
  - body
    `{"goal_slug":"goals/d837ac94-36f5-4735-93bb-d84c69b45435","action":"assign"}`.
- The fixture returned `verified:true` for Agent `agents/tammy`; the UI showed
  `Default Goal owner verified: Tammy.` and ran its refresh path.
- Direct live readback afterward still showed Entrepreneurship missing its
  owner, proving the intercepted POST did not reach live Mission Control or
  mutate GBrain during QA.
- V0.0.186 Task and Goal summary links still opened the exact canonical detail,
  focused the H2 title, and restored focus to the original summary control on
  Close after 1900ms on desktop and mobile.
- Fresh live desktop/mobile sessions had zero console warnings/errors, zero
  failed requests, zero HTTP responses at or above 400, GET-only app API
  methods, and zero GBrain mutations. The only non-GET was the separately
  isolated assignment fixture captured before network.
- Landed commit:
  `a68b8febbd7874999ea5f61d3439a5d34bd69fc5`
  (`Assign missing goal owners from summary`).
