# V0.0.196 — Make Agent answer template concrete

## Scope

- Goal execution waiting-question `Insert answer template` now inserts a
  concrete editable approval draft instead of placeholder choices.
- The template click still only fills and focuses the inline answer textarea.
- Tony must still review and explicitly use `Submit answer` before any answer
  is posted.
- Template insertion does not answer the TODO, assign a Goal owner,
  acknowledge a handoff, wake a worker, complete a task, or mutate GBrain.
- Existing V0.0.195 template button, V0.0.194 exact `next_action`, V0.0.193
  question detail, V0.0.190 inline answer submission, and V0.0.192 owner
  recommendation behavior remain preserved.

## Canonical concrete template

Postdeploy `/api/goal-execution` read back:

- `last_run=true`
- `last_error=null`
- `public_reason=actively_executing`
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- `answer_question` Action queue entry includes concrete `answer_template`

The inserted template text is:

```text
Scope categories: accepted
Desired outcomes: accepted
Constraints: accepted
First action: approved
Notes: Keep the work bounded to the stated scope, outcomes, constraints, and first action.
```

Answer submission remains the separate verified endpoint:

```text
POST /api/todos/<todo>/answer
```

with `answer`, `expected_updated_at`, `actor=people/tony-guan`,
`source=mission_control`, and UUID `idempotency_key`.

## Verification

- Focused Goal execution/frontend contract — 2 OK.
- Combined focused suite:
  `python3 -m unittest tests.test_goal_execution tests.test_frontend_contract`
  — 238 OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Independent desktop and genuine mobile QA passed for aggregate
  `c2aab1808f0290eba4973b3452affda7c81d588637c5c3cbd6cdf8707fbf49dc`.
- QA report: `artifacts/qa/v0.0.196-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.196-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.196.
- Desktop 1440x1000 and mobile 390x844 each showed the full Artifact-backed
  Family/Toddy question detail, exactly one labeled inline answer textarea,
  `Submit answer`, and exactly one accessible `Insert answer template` button
  without overflow.
- Activating `Insert answer template` inserted the exact concrete template
  above.
- No placeholder strings remained.
- The textarea received focus on both widths.
- Template activation emitted zero POST/non-GET requests and did not submit or
  mutate data.
- A separately authorized answer submit was intercepted before live delivery
  at `/api/todos/todos%2F99b64fec-aebe-57de-bf79-cc9d640a2db2/answer` with
  the exact answer, `expected_updated_at`, actor `people/tony-guan`, source
  `mission_control`, and a nonempty UUID idempotency key.
- Direct canonical readback afterward remained unchanged: Task blocked with
  unchanged `updated_at`; TODO `not_done`, unchanged `updated_at`, and no
  answer.
- Timmy was the single recommended owner. Assignment controls remained
  Codex-only Timmy/Toddy/Tammy, with no OpenClaw assignment control.
- The direct `Answer Agent question` action opened the exact Family Task
  `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`, focused
  `#task-handoff-answer`, and restored the exact immutable
  `.goal-execution-answer-action` origin on both widths after at least 1900 ms.
- Fresh browser checks recorded zero console warnings/errors, zero failed
  requests, zero HTTP responses at or above 400, and zero normal live non-GET
  requests. The only observed non-GET was the explicitly intercepted answer
  fixture, which did not reach GBrain.
- Landed commit:
  `78e57d2313690544a2f34957f947bea08640c310`
  (`Make agent answer templates concrete`).
