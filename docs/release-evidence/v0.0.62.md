# V0.0.62 release evidence

Date: 2026-08-01

Scope:

- System Ticket cards open a selected right-side detail panel and a
  ticket-specific editor backed by same-slug verified GBrain writes.
- All Mission Control tooltips use one fixed Memory Stargraph-style HUD
  overlay with delegated hover and keyboard-focus behavior.
- GBrain status and System Tickets flank the left side of the independently
  centered word-art row; Logs and About flank the right side.
- Inbox uses one original source-controlled envelope/check SVG inspired by the
  retained canonical reference attachment.

Validation:

- Complete automated suite: 258 tests passed. Python/JavaScript syntax and
  `git diff --check` passed.
- Dashboard-managed `gtasks` restarted successfully and
  `http://127.0.0.1:4179/api/health` reported `V0.0.62`.
- Independent pre-commit UI/UX QA explicitly returned PASS at desktop
  1440 x 1000 and genuine mobile 390 x 844 after three discovered defects
  were repaired and retested: SVG static serving, stale HUD cleanup/focused
  HUD restoration, and mobile version/art overlap.
- Desktop and mobile document widths exactly matched their 1440px and 390px
  viewports. At 390px, artwork ended at y=2165.515625 and the version label
  began at y=2167.515625, leaving a 2px non-overlapping gap; all four footer
  controls had empty intersection sets.
- Inbox normal, hover, keyboard-focus, and active states passed with its
  accessible label and non-color active marker intact.
- Refresh retained a single viewport-clamped HUD through live rerenders, while
  disconnected ticket-card targets cleared stale HUD state. Representative
  static/dynamic controls, System Ticket detail/editor-cancel behavior, and
  narrow-screen focused detail presentation passed.
- Final fresh desktop and mobile sessions recorded zero console errors and all
  recorded requests completed with 2xx responses.

Rendered evidence is retained in
`artifacts/v0.0.62-qa/`, including
`final-gate-desktop-detail.png`, `final-gate-desktop-refresh.png`,
`final-gate-mobile-detail.png`, `final-gate-mobile-footer.png`, and
`final-gate-mobile-refresh.png`.

Inbox reference provenance:

- Canonical ticket attachment:
  `Screenshot-2026-07-31-at-11.16.07-PM.png`
- Verified on the GBrain host: 18,593 bytes.
- SHA-256:
  `20f2ff2e1da6aca3e28ba6b7e8430c4e3e74bc814a27595d2f677308bda0407b`.
