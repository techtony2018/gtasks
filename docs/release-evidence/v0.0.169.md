# Mission Control V0.0.169 — Populate waiting Goal task context

Date: 2026-08-24

## Scope

- Waiting-for-Tony Goal execution headline now carries populated selected Task
  context: `slug`, `title`, `status`, and `agent_slug`.
- Agents cold-load UI uses `goal-execution.last_run.task` before the separate
  Agent Work cache has reconciled, so Family/Toddy can show the exact Task
  link immediately.
- Local-vs-remote worker boundary remains unchanged: this Mac has local
  configs only for `agents/tammy` and `agents/tammy-oc`; Timmy/Toddy workers
  belong on their own hosts.

## Release

- Commit: `066bca00433deb313b79b2383b0156677c38c2e6`
- Message: `Populate waiting Goal task context`
- Push: `origin/main` updated from `b3e9257` to `066bca0`
- Dashboard-managed `/api/health` and `/api/releases` read back `V0.0.169`
  after deploy.

## Independent QA

PASS recorded at:

- `/Users/tony/work/gtasks/artifacts/qa/v0.0.169-independent/gate-report.md`
- `/Users/tony/work/gtasks/artifacts/qa/v0.0.169-independent/gate-results.json`

QA covered:

- desktop `1440x1000`
- genuine mobile `390x844`

## Automated contracts

- Developer verification reported focused `128` OK.
- Developer verification reported full regression
  `python3 -m unittest discover -s tests` → `1372` OK with `5` skipped.
- JS syntax check passed.

## Live readback during documentation refresh

- `/api/health` read back `V0.0.169`.
- `/api/releases` read back current version `V0.0.169`.
- `/api/goal-execution` read back `public_reason=waiting_for_tony`,
  `decision_count=13`, and `last_error=null`.
- The selected task context was populated:
  - slug `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
  - title `Prepare family-care goal map and weekly review brief`
  - status `blocked`
  - agent `agents/toddy`

## Operational boundary

This is a readback/display improvement, not task lifecycle repair. The
Family/Toddy task remains blocked until Tony answers the canonical question
TODO and the Agent resumes through the normal handoff path. The local worker
boundary is unchanged: do not document Timmy or Toddy as local workers on this
Mac, and do not install local Timmy/Toddy workers to work around remote-host
availability.
