# Mission Control V0.0.170 — Exclude mention-only legacy goal concepts

Date: 2026-08-25

## Scope

- Goal collection reads now hydrate only typed `member_of` goal backlinks and
  the existing legacy empty-link backlink shape.
- Legacy concept pages under `goals/*` that merely mention
  `collections/tonys-goals` are excluded from Mission Control Goal surfaces
  instead of surfacing false `core_data` errors.
- No Tony goal pages were migrated or mutated for this release.

## Canonical trigger

- System Ticket:
  `tasks/1c0fc43e-4886-4389-a237-48f2de6ed1c3`
- Current live defect before repair:
  `/api/tasks?refresh=1` reported exact `core_data` fingerprints for
  `goals/friendship` and `goals/entrepreneurship-startup-ideas`.
- Direct GBrain readback showed both pages are `type=concept` with no canonical
  Goal fields and only `mentions -> collections/tonys-goals` relationships.

## Developer verification

- RED:
  `tests.test_gbrain.GoalReadTests.test_ignores_goal_like_concepts_that_only_mention_goal_root`
  failed because `list_goals()` attempted to hydrate the mentions-only
  `goals/friendship` page.
- GREEN:
  `python3 -m unittest tests.test_gbrain.GoalReadTests.test_ignores_goal_like_concepts_that_only_mention_goal_root tests.test_gbrain.GoalReadTests.test_discovers_every_direct_goal_backlink_dynamically`
  passed.
- Focused Goal read tests passed:
  `python3 -m unittest tests.test_gbrain.GoalReadTests`
- Focused API/collection regression passed:
  `python3 -m unittest tests.test_gbrain.CollectionReadTests tests.test_gbrain.GoalReadTests tests.test_server.TasksApiTests tests.test_server.GoalRelationshipApiTests tests.test_server.GoalMutationApiTests tests.test_server.TaskRelationshipRepairApiTests`
  → `28` OK.
- Full regression passed:
  `python3 -m unittest discover -s tests` → `1373` OK, `5` skipped.
- JavaScript syntax check passed:
  `node --check static/app.js`.

## Independent QA

PASS recorded at:

- `/Users/tony/work/gtasks/artifacts/qa/v0.0.170-independent/gate-report.md`
- `/Users/tony/work/gtasks/artifacts/qa/v0.0.170-independent/gate-results.json`

Frozen uncommitted aggregate:
`d78f0170483e862629306b4d7f9ab716569667e7023d1f98a93fb678a49484eb`.

QA covered:

- desktop `1440x1000`
- genuine mobile `390x844`
- `/api/tasks?refresh=1` returned `45` last-valid tasks and `issues=0`
- exact legacy fingerprints for `goals/friendship` and
  `goals/entrepreneurship-startup-ideas` had zero hits
- `7` valid Goals rendered
- Today, Goals, All Tasks, Inbox, System Tickets, and About smoke passed
- console/network errors `0`, browser flow GET-only, no GBrain mutation

## Release status

- Product commit:
  `8900b85d9ffd58d8146f8f76ecda6fc6a6060d81`
  (`Exclude mention-only legacy goal concepts`)
- Push: `origin/main` updated to `8900b85`.
- Dashboard-managed restart:
  `POST http://127.0.0.1:4188/api/services/gtasks/restart` returned OK.
- Postdeploy readback:
  `/api/health` and `/api/releases` both returned `V0.0.170`.
- Postdeploy `/api/tasks?refresh=1` returned `issues=0` with no legacy
  concept fingerprint hits.
