# Mission Control V0.0.170 — Exclude mention-only legacy goal concepts

Date: 2026-08-25

## Scope

- Goal collection reads now hydrate only typed `member_of` goal backlinks and
  the existing legacy empty-link backlink shape.
- Legacy concept pages under `goals/*` that merely mention
  `collections/tonys-goals` are excluded from Mission Control Goal surfaces
  instead of surfacing false `core_data` errors.
- No Tony goal pages were migrated or mutated for this release.

## Canonical trigger

- System Ticket:
  `tasks/1c0fc43e-4886-4389-a237-48f2de6ed1c3`
- Current live defect before repair:
  `/api/tasks?refresh=1` reported exact `core_data` fingerprints for
  `goals/friendship` and `goals/entrepreneurship-startup-ideas`.
- Direct GBrain readback showed both pages are `type=concept` with no canonical
  Goal fields and only `mentions -> collections/tonys-goals` relationships.

## Developer verification

- RED:
  `tests.test_gbrain.GoalReadTests.test_ignores_goal_like_concepts_that_only_mention_goal_root`
  failed because `list_goals()` attempted to hydrate the mentions-only
  `goals/friendship` page.
- GREEN:
  `python3 -m unittest tests.test_gbrain.GoalReadTests.test_ignores_goal_like_concepts_that_only_mention_goal_root tests.test_gbrain.GoalReadTests.test_discovers_every_direct_goal_backlink_dynamically`
  passed.
- Focused Goal read tests passed:
  `python3 -m unittest tests.test_gbrain.GoalReadTests`

## Release status

- Commit, push, dashboard-managed deploy, independent QA, and canonical ticket
  completion receipts are pending at the time this evidence file is created.
