# V0.0.194 — Make Goal next action exact

## Scope

- Goal execution `summary.next_action` now names the exact Tony-owned actions
  instead of generic guidance.
- When both a waiting Agent question and a missing-owner repair are present,
  the line names:
  - the exact waiting Agent question Tony should answer;
  - the recommended missing-owner assignment from the verified Action queue.
- The current live summary names the Family/Toddy question and recommends
  assigning the Entrepreneurship Goal to Timmy with
  `recommended: lowest verified Codex Goal load`.
- This is guidance only. It does not automatically answer a TODO, assign a
  Goal owner, acknowledge a handoff, wake a worker, complete a task, or mutate
  GBrain.
- Existing V0.0.193 question detail, V0.0.190 inline answer composer, and
  V0.0.192 owner recommendation behavior remain preserved.

## Canonical next action

Postdeploy `/api/goal-execution` read back this exact `summary.next_action`:

```text
Answer the Toddy question for Which family-care scope, outcomes, constraints, and first action should Toddy use next? and assign Entrepreneurship: create a company and start running business, compound over time to Timmy (recommended: lowest verified Codex Goal load); executing or delivered Agent work can continue.
```

Supporting readback:

- `last_run=true`
- `last_error=null`
- `public_reason=actively_executing`
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- `summary.total_goals=7`
- Question task: `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
- Question:
  `Which family-care scope, outcomes, constraints, and first action should Toddy use next?`
- Missing-owner Goal:
  `Entrepreneurship: create a company and start running business, compound over time`
- Recommended owner:
  Timmy, `recommended: lowest verified Codex Goal load`

## Verification

- Focused suite:
  `python3 -m unittest tests.test_goal_execution tests.test_frontend_contract`
  — 238 OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Independent desktop and genuine mobile QA passed for aggregate
  `c83433e7bf9169b453651f948d9d83ef3b7738f38978e61a250a0db782ae3e0e`.
- QA report: `artifacts/qa/v0.0.194-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.194-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.194.
- Desktop 1440x1000 and mobile 390x844 visibly rendered the exact next action
  text shown above.
- The Action queue simultaneously retained the V0.0.193 verified question
  detail beginning
  `Based on Artifact artifacts/0e0323e7-f5b8-4833-881a-018507ac7e2a` beside
  its single inline answer form.
- Owner controls remained `Assign to Timmy`, `Assign to Toddy`, and
  `Assign to Tammy`, with exactly one Timmy recommendation and no OpenClaw
  assignment control.
- QA intercepted the answer POST before live network; it retained exact
  `expected_updated_at`, `actor=people/tony-guan`, `source=mission_control`,
  and a nonempty UUID idempotency key.
- Canonical readback afterward remained Task blocked, TODO `not_done`, and
  answer null.
- Direct answer action still opened exact Family Task
  `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`, focused
  `#task-handoff-answer`, and restored the exact immutable origin after 1900 ms
  on both widths.
- Fresh browser checks recorded zero console warnings/errors, zero failed
  requests, zero HTTP responses at or above 400, and normal live GET-only
  browser traffic. The only POST was intercepted in-memory.
- Landed commit:
  `06446ef40abba67eac8b8f9732fb0408a2643b04`
  (`Make goal next action exact`).
