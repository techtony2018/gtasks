# V0.0.214 — Classify unavailable Goal workers separately

## Scope

- Product commit:
  `0a35c1e667a9148426f473075a865047335ea144`.
- Mission Control preserves dispatcher `execution_claim_unavailable` reasons
  in latest delivery state.
- Stale or expired claim-unavailable delivery now classifies as
  `handoff_worker_unavailable` instead of generic `handoff_needs_repair`.
- Goal execution surfaces a system-owned `restore_agent_worker` Action queue
  item with label `Restore verified Agent worker`.
- Active-claim precedence remains: a suppressed attention row with a recent,
  future-expiring, non-terminal execution claim still renders Delivering.

## Independent QA

- Verdict: PASS.
- Frozen aggregate:
  `489409ceaa22c43797a603f2b66393711599f074e12e1b6065d022c75f93c49a`.
- QA report:
  `artifacts/qa/v0.0.214-independent/gate-report.md`.
- QA results:
  `artifacts/qa/v0.0.214-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About reported
  V0.0.214.
- Focused V0.0.214 regressions passed: three Goal execution tests plus one
  canonical handoff bridge test.
- Desktop 1440x1000 and genuine mobile 390x844 both showed the system-owned
  restore action in Agents and Inbox with exact label, summary, and detail.
- The restore item showed zero Tony answer forms, answer templates, owner
  assignment controls, and OpenClaw assignment controls.
- Active-claim precedence, generic handoff repair, Artifact/private, and
  Codex-owner regressions passed.
- Live navigation/readback traffic was GET-only; non-GET count was zero.

## Live readback

- `/api/goal-execution?refresh=1` returned `last_error=null`,
  `public_reason=actively_executing`, and Health/Toddy task
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5` remained visible as active
  work.
- Summary readback: `total_goals=7`, `needs_attention=1`,
  `waiting_for_tony=0`, `in_flight=1`, and `recently_completed=5`.
- Reasons readback: `handoff_worker_unavailable=1`,
  `recently_completed=5`, and `duplicate=1`.
- `summary.action_queue` contained exactly one system-owned
  `restore_agent_worker` item for Family/Toddy task
  `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d` and Goal
  `goals/2c86f86c-c9fb-5f49-96d0-e4d63f489fc8`.
- `summary.next_action` read
  `restore verified Agent worker for 1 blocked Goal`.

## Verification

- Developer focused coverage: `417` OK.
- Developer full suite:
  `python3 -m unittest discover -s tests` = `1408` OK with `5` skipped.
