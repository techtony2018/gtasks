# V0.0.193 — Show Agent question detail in Action queue

## Scope

- Goal execution Action queue `answer_question` entries now carry verified
  waiting-for-Tony question detail.
- The Agents UI renders that detail beside the inline answer form, so Tony can
  answer waiting Agent blockers directly from the Action queue without opening
  the Task first.
- The visible Family/Toddy detail begins
  `Based on Artifact artifacts/0e0323e7-f5b8-4833-881a-018507ac7e2a`.
- This is read-only guidance plus explicit user answer submission only. It
  does not automatically answer the TODO or mutate GBrain.
- Existing V0.0.190 inline answer composer, V0.0.189 direct answer action, and
  V0.0.192 owner recommendation behavior remain preserved.

## Canonical answer target

- Action kind: `answer_question`
- Task: `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
- TODO: `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`
- Agent: `agents/toddy`
- Question:
  `Which family-care scope, outcomes, constraints, and first action should Toddy use next?`
- Detail prefix:
  `Based on Artifact artifacts/0e0323e7-f5b8-4833-881a-018507ac7e2a`
- Answer endpoint remains:
  `POST /api/todos/<todo>/answer`
- Answer POST fields remain:
  `answer`, `expected_updated_at`, `actor=people/tony-guan`,
  `source=mission_control`, and UUID `idempotency_key`.
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- Postdeploy public reason: `actively_executing`

## Verification

- Focused suite:
  `python3 -m unittest tests.test_goal_execution tests.test_frontend_contract`
  — 238 OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Independent desktop and genuine mobile QA passed for aggregate
  `c46acb37d259f543397a81ff5592a7c0233bc43cebe4951bfb62bf34f399b7eb`.
- QA report: `artifacts/qa/v0.0.193-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.193-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.193.
- Postdeploy `/api/goal-execution` readback had:
  - `last_run=true`;
  - `last_error=null`;
  - `public_reason=actively_executing`;
  - selected task `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`;
  - `summary.total_goals=7`;
  - Family/Toddy `answer_question` detail beginning
    `Based on Artifact artifacts/0e0323e7-f5b8-4833-881a-018507ac7e2a`.
- Desktop 1440x1000 and mobile 390x844 each rendered exactly one Action queue
  inline answer form with one labeled textarea and one `Submit answer` control.
- The short question stayed visible and the verified full detail appeared
  beside the form.
- Accessible textarea name:
  `Answer Agent question: Which family-care scope, outcomes, constraints, and first action should Toddy use next?`
- QA intercepted the answer POST before live network at
  `/api/todos/todos%2F99b64fec-aebe-57de-bf79-cc9d640a2db2/answer`; captured
  JSON included the submitted answer, exact
  `expected_updated_at=2026-08-24T04:16:56.207044-07:00`,
  `actor=people/tony-guan`, `source=mission_control`, and a nonempty UUID
  idempotency key.
- Canonical readback remained unchanged afterward: Task blocked, TODO
  `not_done`, identical timestamps, answer null, and no live GBrain mutation.
- V0.0.192 owner controls remained Timmy/Toddy/Tammy with exactly one Timmy
  recommendation and no OpenClaw assignment control.
- V0.0.190 direct action regression stayed intact: `Answer Agent question`
  opened exact Task `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`, focused
  `#task-handoff-answer`, and restored the exact immutable Action queue
  origin on Close.
- Fresh browser checks recorded zero console warnings/errors, zero failed
  requests, zero HTTP responses at or above 400, and normal live GET-only
  browser traffic. The single non-GET was intercepted in-memory and never
  reached GBrain.
- Landed commit:
  `bdf197b7000eec23d783f04756d3d17da7d81345`
  (`Show agent question detail in action queue`).
