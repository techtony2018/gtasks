# V0.0.191 — Assign Goal owners from action queue

## Scope

- Goal execution Action queue `assign_goal_owner` entries now render inline
  assignment controls directly in the primary Action queue.
- Inline controls are Codex-only:
  - `Assign to Tammy`;
  - `Assign to Toddy`;
  - `Assign to Timmy`.
- The controls use the existing verified
  `POST /api/agents/<agent>/default-goals` contract with
  `{goal_slug, action: "assign"}`.
- OpenClaw assignment remains excluded.
- The separate Missing owner detail row keeps its own Tammy/Toddy/Timmy
  controls.
- Existing V0.0.190 inline answer composer and V0.0.189 direct answer action
  behavior remain preserved.

## Canonical assignment target

- Action kind: `assign_goal_owner`
- Goal: `goals/d837ac94-36f5-4735-93bb-d84c69b45435`
- Relationship: `default_agent_for`
- Desktop intercepted endpoint:
  `/api/agents/agents%2Ftammy/default-goals`
- Mobile intercepted endpoint:
  `/api/agents/agents%2Ftimmy/default-goals`
- POST body:
  `{ "goal_slug": "goals/d837ac94-36f5-4735-93bb-d84c69b45435", "action": "assign" }`
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- Postdeploy public reason: `actively_executing`

## Verification

- Focused suite:
  `python3 -m unittest tests.test_goal_execution tests.test_frontend_contract`
  — 238 OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Independent desktop and genuine mobile QA passed for the unchanged aggregate
  `9e2486934841d4a59b1c0789f1c718b3945624372e2334081847d313ee71c299`.
- QA report: `artifacts/qa/v0.0.191-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.191-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.191; About showed `Assign Goal owners from action queue`.
- Postdeploy `/api/goal-execution` readback had:
  - `last_run` present;
  - `last_error=null`;
  - `public_reason=actively_executing`;
  - selected task `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`;
  - `summary.total_goals=7`;
  - Action queue entries for `answer_question` and Entrepreneurship
    `assign_goal_owner`.
- QA used a source-blind intercepted Goal execution GET fixture because the
  managed scheduler was cold after restart and the live read initially had no
  `last_run`; the fixture did not write to live data.
- Desktop and mobile both showed `Assign to Tammy`, `Assign to Toddy`, and
  `Assign to Timmy` inline beside the Action queue `Assign Goal owner` item.
- The separate Missing owner detail row retained its own Tammy/Toddy/Timmy
  controls.
- No OpenClaw assignment button appeared.
- Six verified Agent cards remained rendered and the V0.0.190 inline answer
  form stayed present.
- QA intercepted assignment POSTs before live network:
  - desktop captured `/api/agents/agents%2Ftammy/default-goals`;
  - mobile captured `/api/agents/agents%2Ftimmy/default-goals`;
  - both used method `POST` and the exact Entrepreneurship
    `{goal_slug, action: "assign"}` body.
- A canonical-readback-shaped synthetic response produced the verified toast
  `Default Goal owner verified: Tammy.` and exercised the refresh path.
- Direct live readback afterward still showed the Entrepreneurship Goal with
  `default_agent`, `default_agent_for`, and `owner_agent` all null, proving the
  intercepted POSTs did not reach GBrain.
- V0.0.189 answer-action regression stayed intact: `Answer Agent question`
  opened exact Task `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`, focused
  `TEXTAREA#task-handoff-answer`, and Close restored the exact
  `.goal-execution-answer-action` origin after 1.9 seconds.
- Fresh browser checks recorded zero console warnings/errors, zero failed
  requests, zero HTTP responses at or above 400, normal live GET-only browser
  traffic, and no GBrain mutations. Intercepted fixture POSTs did not reach
  live GBrain.
- Landed commit:
  `21aeac225d4b344a3a3c0ba1f984c1f5b6769668`
  (`Assign goal owners from action queue`).
