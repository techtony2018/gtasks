# V0.0.189 — Focus Goal question actions on answers

## Scope

- Goal execution Action queue `Answer Agent question` is now a direct inline
  action.
- Activating it opens the exact canonical waiting-for-input Task after readback
  and focuses existing `#task-handoff-answer`.
- Close restores focus to the exact originating `.goal-execution-answer-action`
  using immutable `data-goal-execution-origin`, not a same-slug Agent-card
  link.
- No new mutation path was added. Actual answer submission remains the existing
  verified `/api/todos/<todo>/answer` flow.
- Assignment controls remain Codex-only: `Assign to Tammy`, `Assign to Timmy`,
  and `Assign to Toddy`; no OpenClaw assignment control is exposed.

## Canonical action queue

- Tony `answer_question`:
  - Task: `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
  - TODO: `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`
  - Focus target: `#task-handoff-answer`
  - Origin control: `.goal-execution-answer-action`
- Tony `assign_goal_owner`:
  - Goal: `goals/d837ac94-36f5-4735-93bb-d84c69b45435`
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- Postdeploy public reason: `actively_executing`

## Verification

- RED reproduced the missing direct action.
- MC189-001 reproduced missing immutable focus origin, then repaired.
- Focused suite:
  `python3 -m unittest tests.test_goal_execution tests.test_frontend_contract`
  — 238 OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Repaired independent desktop and genuine mobile QA passed for the unchanged
  frozen aggregate
  `4a51b29c345b9043a3eee7cbe7945d2050dfb446c775043bc56c392222f13f97`.
- QA report:
  `artifacts/qa/v0.0.189-repair-independent/gate-report.md`.
- QA results:
  `artifacts/qa/v0.0.189-repair-independent/gate-results.json`.
- Dashboard-managed `/api/health` and `/api/releases` read back V0.0.189.
- Postdeploy `/api/goal-execution` readback had:
  - `last_run` present;
  - `public_reason=actively_executing`;
  - selected task `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`;
  - Tony `answer_question` action for
    `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`;
  - Tony `assign_goal_owner` action for
    `goals/d837ac94-36f5-4735-93bb-d84c69b45435`;
  - `last_error=null`.
- Desktop and mobile verified one activation of `Answer Agent question` opened
  the exact Task `Prepare family-care goal map and weekly review brief` and
  focused `TEXTAREA#task-handoff-answer` after canonical readback. The textarea
  stayed empty; QA did not type or submit an answer.
- After Close and a 1.9 second rerender window, focus returned to the exact
  originating `.goal-execution-answer-action` on both desktop and mobile, not
  to an Agent-card `.inline-task-link`.
- The immutable origin identity was:
  `summary:action:answer_question:tasks/561640dd-8e34-43e1-a03e-e3f3f270033d:todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`.
- Regression checks confirmed six Agent cards, visible Goal execution counts,
  action queue, Family question, missing-owner copy, and `default_agent_for`
  guidance.
- Assignment controls were exactly `Assign to Tammy`, `Assign to Toddy`, and
  `Assign to Timmy`; no OpenClaw assignment control appeared.
- Fresh browser sessions recorded zero console warnings/errors, zero failed or
  HTTP responses at or above 400, only GET live app methods, and no GBrain
  mutation.
- Landed commit:
  `748fa8a0bfbd0ec0fa648a1be4f181658d62c609`
  (`Focus goal question actions on answers`).
