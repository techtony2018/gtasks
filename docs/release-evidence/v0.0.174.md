# V0.0.174 — Recover checkpointed Goal handoffs

## Scope

- Requeue only the same active/planned Goal-derived task when its latest verified handoff is `suppressed` with a recoverable execution checkpoint or expiry.
- Preserve canonical owner, task slug, Goal derivation, fixed Agent route, and the existing handoff/idempotency key.
- Keep waiting-for-Tony, missing-next-action, non-derived, and unsupported repair states in Needs attention.
- Render `retrying` handoffs as Delivering so recovered work does not continue to look stuck.

## Verification

- RED: `python3 -m unittest tests.test_goal_execution.GoalExecutionEngineTests.test_canary_requeues_recoverable_existing_handoff_repair`
- Focused: `python3 -m unittest tests.test_goal_execution tests.test_handoff_dispatcher.HandoffDispatcherTests.test_canonical_bridge_retries_latest_task_handoff_recovery tests.test_handoff_dispatcher.HandoffDispatcherTests.test_operator_recovery_requeues_checkpointed_owned_execution tests.test_frontend_contract.FrontendContractTests.test_goal_execution_runtime_maps_public_states_and_keeps_last_valid_on_error`
- Static: `node --check static/app.js`

## QA

- Independent desktop 1440x1000 and genuine mobile 390x844 QA required before commit because the recovered state is UI-visible.
