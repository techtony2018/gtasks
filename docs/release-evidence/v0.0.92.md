# Mission Control V0.0.92 release evidence

V0.0.92 repairs the reopened long-lived Task detail selection regression for
System Ticket `tasks/b923e27b-1ed3-491c-95b9-b6e191e97cd1`.

## Scope

- Preserve the existing first-click Task detail busy panel introduced earlier.
- When the same Task already has a canonical detail read in flight but session
  churn, refresh, or rerender leaves the detail panel hidden or unselected,
  reopen the existing busy panel immediately without starting another GBrain
  read.
- Keep duplicate same-task activations coalesced to one canonical read.
- Preserve final exact Task readback reconciliation and stale response guards.

## Developer validation before independent QA

- RED observed: `test_select_task_reopens_busy_detail_when_same_task_read_is_still_in_flight`
  failed because the detail panel did not reopen for the same in-flight Task.
- GREEN observed: the new regression and existing immediate busy-detail test pass.
- Focused frontend contract suite: `python3 -m unittest tests.test_frontend_contract`
  passed 160 tests.

## Required independent QA gate

This is UI-affecting. Commit is blocked until independent UI/UX QA explicitly
passes desktop 1440x1000 and genuine 390x844 against the frozen uncommitted
candidate. QA must verify All Tasks first-click behavior after a long-lived
session/refresh cycle, immediate busy panel/sheet, same-task click coalescing,
and final loaded/error state stability without live GBrain mutation.
