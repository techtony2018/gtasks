# V0.0.200 — Name private blockers in Goal next action

## Scope

- Product commit:
  `5560d8f342674e29a3eac97dec3c3ce3f566027f`.
- Goal execution next-action copy now includes private-input blockers alongside
  ordinary answer and owner actions.
- Inbox and Agents show every reason Goal-derived Agent work is blocked,
  including private credential/token blockers.
- Private blockers are guidance only: Mission Control still does not generate,
  autofill, submit, or one-click run secrets.
- Private `answer_question` rows remain excluded from synthetic
  `answer_template`, inline answer forms, template insertion, and recommended
  unblock plans.

## Postdeploy readback

- Dashboard-managed Mission Control reported V0.0.200 from `/api/health` and
  `/api/releases`.
- `/api/goal-execution` after bounded warmup read back:
  - `last_run=true`
  - `last_error=null`
  - `public_reason=actively_executing`
  - `next_action_has_private=true`

## Repair note

- Initial independent QA failed MC200-001 because Inbox omitted the verified
  `next_action` line.
- The repaired candidate renders `Next action:` in both Agents and Inbox.
- Repaired independent QA PASS aggregate:
  `8672a12899cf49ce341a6e0caff1ce082604328bfb46ff812da080557042b3d3`.
- QA report:
  `artifacts/qa/v0.0.200-independent/gate-report-retest.md`.
- QA results:
  `artifacts/qa/v0.0.200-independent/gate-results-retest.json`.

## Independent QA coverage

- Desktop 1440x1000 and genuine mobile 390x844 both rendered the exact
  verified `Next action:` on Agents and Inbox.
- Both surfaces included the ordinary Family/Toddy question, `provide private
  input for the Tammy question`, and exact private question summary
  `Provide the production API access token and OAuth client secret.`
- No synthetic credential value was generated or displayed.
- The private item remained labeled `Private input required`, with no answer
  form, template button, raw TODO version, or synthetic credential value.
- `Run recommended unblock plan` remained available for the safe Family answer
  plus recommended Timmy owner assignment.
- On both desktop and mobile, the plan emitted exactly two intercepted writes
  in order: safe Family TODO answer, then Timmy Goal-owner assignment.
- Catch-all private TODO route recorded zero attempts.
- Direct Answer still opened the exact Family Task, focused
  `#task-handoff-answer`, and Close restored immutable origin focus.
- Scoped fresh browser readbacks recorded no warnings/errors or unexpected
  non-GET requests; no GBrain mutation occurred.
- Independent Goal-execution/frontend/release tests: `321` passed.

## Developer verification

- Focused repair checks: `2` OK.
- Focused Goal execution and frontend contract:
  `python3 -m unittest tests.test_goal_execution tests.test_frontend_contract`
  = `240` OK.
- Full suite: `python3 -m unittest discover -s tests` = `1396` OK / `5`
  skipped.
