# V0.0.192 — Recommend Goal owner assignments

## Scope

- Goal execution missing-owner summaries and Action queue `assign_goal_owner`
  entries now include verified Codex Agent candidate-owner metadata.
- The Agents UI labels exactly one recommended owner by lowest verified Codex
  Goal load.
- The current Entrepreneurship missing-owner repair shows:
  - Timmy with verified default Goal count 1 and
    `recommended: lowest verified Codex Goal load`;
  - Toddy with verified default Goal count 2;
  - Tammy with verified default Goal count 3.
- No automatic `default_agent_for` mutation is performed. Tony must explicitly
  activate a verified Codex assignment control.
- OpenClaw assignment controls remain excluded.
- Existing V0.0.190 inline answer composer and V0.0.191 Action queue
  assignment controls remain preserved.

## Canonical recommendation target

- Action kind: `assign_goal_owner`
- Goal: `goals/d837ac94-36f5-4735-93bb-d84c69b45435`
- Goal title: `Entrepreneurship: create a company and start running business, compound over time`
- Relationship: `default_agent_for`
- Recommended candidate:
  - Agent: Timmy
  - Verified default Goal count: 1
  - Recommendation: `recommended: lowest verified Codex Goal load`
- Other candidates:
  - Toddy, verified default Goal count 2
  - Tammy, verified default Goal count 3
- Assignment endpoint remains:
  `POST /api/agents/<agent>/default-goals`
- POST body remains:
  `{ "goal_slug": "goals/d837ac94-36f5-4735-93bb-d84c69b45435", "action": "assign" }`
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- Postdeploy public reason: `actively_executing`

## Verification

- Focused suite:
  `python3 -m unittest tests.test_goal_execution tests.test_frontend_contract`
  — 238 OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Independent desktop and genuine mobile QA passed for aggregate
  `f96790cc11fcda7278bbac2bba4f2344e2baec96691f86b71ab93e7806f8715f`.
- QA report: `artifacts/qa/v0.0.192-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.192-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.192.
- Postdeploy `/api/goal-execution` readback had:
  - `last_run=true`;
  - `last_error=null`;
  - `public_reason=actively_executing`;
  - selected task `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`;
  - `summary.total_goals=7`;
  - `assign_goal_owner` for Entrepreneurship with three verified Codex
    candidates: Timmy count 1 recommended, Toddy count 2, Tammy count 3.
- Desktop 1440 and mobile 390 checks showed the recommended Timmy label in
  both the primary Action queue and the Missing owner detail controls.
- No OpenClaw assignment button appeared.
- QA intercepted the assignment POST before live network; the no-write check
  confirmed live readback still showed the Entrepreneurship Goal as missing an
  owner afterward.
- The V0.0.190 answer regression stayed intact: the inline answer form
  remained available, and `Answer Agent question` still opened the exact
  Family/Toddy Task and focused the answer field.
- Fresh browser checks recorded clean console and network results.
- Landed commit:
  `3ad6ea396c26a2c4073eb27e782991313ccbc19f`
  (`Recommend goal owner assignments`).
