# Mission Control V0.0.165 — Make missing Goal owners actionable

Date: 2026-08-24

## Scope

- Clarify Goal execution `owner_missing` Needs attention copy.
- The repair is exact: assign exactly one Codex Agent and verify the
  `default_agent_for` link for the Goal.
- Do not infer Goal ownership from prose, task assignment, Agent profile
  presence, or local worker installation.

## Release

- Commit: `4778793e60bb201393afbafc89a2e81079229d9d`
- Message: `Clarify missing Goal owner repair`
- Dashboard-managed `/api/health` and `/api/releases` read back `V0.0.165`
  after deploy.

## Independent QA

PASS recorded at:

- `/Users/tony/work/gtasks/artifacts/qa/v0.0.165-independent/gate-report.md`
- frozen aggregate `261c5f4e5b9b0d990088ca0a4f56169c614580d0b1f48b351bab85d58b917fb1`

QA verified:

- read-only `/api/goal-execution` projected
  `goals/d837ac94-36f5-4735-93bb-d84c69b45435` as `owner_missing` with no
  fabricated task slug
- desktop `1440x1000` and genuine mobile `390x844` rendered the Goal as Needs
  attention
- visible copy contains both required phrases: `Assign exactly one Codex Agent`
  and `default_agent_for`
- no stack trace, OAuth error, remote-MCP configuration error, or placeholder
  exception text appeared
- local handoff supervisor readback still contains exactly `agents/tammy` and
  `agents/tammy-oc`; Timmy and Toddy are not local workers on this Mac

## Automated contracts

- `python3 -m unittest tests.test_frontend_contract tests.test_releases`:
  `263` passed.

## Operational boundary

This is a copy/repair-instruction release, not a Goal ownership mutation. The
Documentation Manager must not repair missing ownership by creating links. The
implementation owner or authorized operator must create exactly one verified
`default_agent_for` relationship through the supported canonical contract.
