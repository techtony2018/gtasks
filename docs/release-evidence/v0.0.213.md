# V0.0.213 — Queue concrete Goal handoff repair actions

## Scope

- Product commit:
  `b405d642dc61812ba983ed822e362b3868384411`.
- Goal execution converts `handoff_needs_repair` attention into an explicit
  system-owned Action queue item.
- The queued item uses:
  - `owner=system`
  - `kind=repair_agent_handoff`
  - `label=Repair verified Agent handoff`
  - `summary=Inspect Handoff History and recover the verified Agent delivery state.`
- `summary.next_action` names the concrete system recovery:
  `repair verified Agent handoff for 1 blocked Goal`.
- The repair action is system recovery guidance, not a Tony answer form,
  answer template, owner assignment, OpenClaw control, or automatic repair
  mutation.

## Independent QA

- Verdict: PASS.
- Frozen aggregate:
  `edd57f5ab7f4248fe72b0b03a6a870cf19d503052c587b3aee22fe301396e072`.
- QA report:
  `artifacts/qa/v0.0.213-independent/gate-report.md`.
- QA results:
  `artifacts/qa/v0.0.213-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About reported
  V0.0.213.
- Focused regression
  `GoalExecutionEngineTests.test_goal_execution_routes_handoff_repair_attention_to_system_action`
  passed, `1` OK.
- Desktop 1440x1000 and genuine mobile 390x844 both showed the system-owned
  repair action in Agents and Inbox with exact label, summary, and detail.
- The repair action showed zero Tony answer forms, answer templates, owner
  assignment controls, and OpenClaw assignment controls.
- Live navigation/readback traffic was GET-only; non-GET count was zero.

## Live readback

- `/api/goal-execution?refresh=1` returned `last_error=null`,
  `public_reason=actively_executing`, and Health/Toddy task
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5` remained visible as active
  work.
- Summary readback: `total_goals=7`, `needs_attention=1`,
  `waiting_for_tony=0`, `in_flight=1`, and `recently_completed=5`.
- `summary.action_queue` contained exactly one system-owned
  `repair_agent_handoff` item for Family task
  `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d` and Goal
  `goals/2c86f86c-c9fb-5f49-96d0-e4d63f489fc8`.

## Verification

- Developer focused coverage: `328` OK.
- Developer full suite:
  `python3 -m unittest discover -s tests` = `1405` OK with `5` skipped.
- Independent QA focused regression: `1` OK.
