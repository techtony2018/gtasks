# Mission Control V0.0.167 — Prioritize actionable Goal blockers

Date: 2026-08-24

## Scope

- Auto Goal execution public selection now surfaces actionable blocker states
  before `recently_completed` history.
- Active or eligible work still wins: `auto_eligible` activation remains first,
  followed by existing duplicate/recent work with an accepted active dispatcher
  handoff.
- The actionable blocker states are `waiting_for_tony`,
  `handoff_needs_repair`, `handoff_missing`, `task_needs_next_action`, and
  `handoff_worker_unavailable`.

## Release

- Commit: `5a3a51c81196cfcfdcbce3722802b90e58271d25`
- Message: `Prioritize actionable Goal execution blockers`
- Dashboard-managed `/api/health` and `/api/releases` read back `V0.0.167`
  after deploy.

## Independent QA

PASS recorded at:

- `/Users/tony/work/gtasks/artifacts/qa/v0.0.167-independent/gate-report.md`
- frozen aggregate `8b44466616156a9491f18db25da846641ecd70bb795f8fb0e5a0d34525a97df6`

QA verified:

- desktop `1440x1000` PASS
- genuine mobile `390x844` PASS

## Automated contracts

- Developer verification reported focused `126` OK.
- Developer verification reported full regression `1371` OK with `5` skipped.

## Live readback during documentation refresh

- `/api/health` read back `V0.0.167`.
- `/api/releases` read back `V0.0.167`.
- `/api/goal-execution` read back `public_reason=waiting_for_tony`,
  `decision_count=13`, and `last_error=null`.

## Operational boundary

This is a public-selection ordering change, not expanded automation authority.
It does not bypass the one-task canary boundary, does not make
`recently_completed` active work, and does not repair Tony-answer, handoff,
missing-next-action, or worker-route blockers without the owner action named
by the selected state.
