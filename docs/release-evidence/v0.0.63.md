# V0.0.63 release evidence

## Scope

- New canonical Goals, Projects, Tasks, and System Tickets receive immutable,
  title-independent opaque UUID slugs.
- Goal reads recognize GBrain's documented raw `concept` + compiled/frontmatter
  `type: goal` representation without weakening Task or Agent type checks.
- The one-time copy/relink migration keeps legacy pages as `canonical_alias_of`
  compatibility entries and normalizes collection membership to one typed edge.

## Independent UI/UX gate

- Independent read-only desktop QA passed against the uncommitted Dashboard
  candidate at `http://127.0.0.1:4179/`.
- QA covered Today, Goals, Projects, Agents, Inbox warnings, System Ticket
  detail/wording, About/version, legacy slug reads, and browser console health.
- Live GBrain data was confirmed unmigrated and QA performed no writes.
- Tony recorded a one-time exception for this candidate: a genuine 390px
  viewport could not be proven by the available backend. This is explicitly
  deferred and is not represented as a mobile QA pass.

## Developer verification

- `python3 -m unittest discover -s tests` — 266 passed.
- `node --check static/app.js` — passed.
- `python3 -m py_compile scripts/migrate_canonical_ids.py gtasks/domain.py gtasks/gbrain.py` — passed.
- Read-only GBrain audit covered 41 mapped entities, identified all six legacy
  untyped Tony's Goals membership edges for repair, and excluded the soft-deleted
  `tasks/2026/2026-07-30-erfa-pac-filing-fc0ba359` task.
