# Mission Control V0.0.166 — Keep Goal execution running past hidden Agent issues

Date: 2026-08-24

## Scope

- Goal execution no longer aborts the entire scheduler for non-visible
  malformed Agent work items that are already reported in Inbox.
- Missing canonical roots and visible unsafe Agent-work issues still fail
  closed.
- Hidden malformed Agent work remains a data-quality warning, not executable
  Board work.

## Release

- Commit: `7b57e945afa70ed47761d20f62be156bb785ee33`
- Message: `Keep Goal execution running past hidden Agent issues`
- Dashboard-managed `/api/health` and `/api/releases` read back `V0.0.166`
  after deploy.

## Independent QA

PASS recorded at:

- `/Users/tony/work/gtasks/artifacts/qa/v0.0.166-independent/gate-report.md`
- frozen aggregate `c9fd2484758bde53b55767930659607c75f8d99dc7cea56bdaab73b789e7adaa`

QA verified:

- read-only `/api/goal-execution` returned a populated 13-decision last run
  with `last_error: null`
- the surface did not fall into a total-unavailable state
- Entrepreneurship remained `owner_missing` with the V0.0.165 actionable
  `Assign exactly one Codex Agent` / `default_agent_for` copy
- read-only `/api/agent-work` returned malformed task
  `tasks/78147b5d-7385-431e-ae1a-cf710a160910` as an error issue with
  `task_visible: false`, while returning 44 valid Agent work items
- desktop and mobile Inbox displayed the exact slug,
  `waiting_for_input requires blocked task status`, and
  `This malformed Tammy work item is reported in Inbox and is not shown on Board.`
- desktop and mobile Board contained neither the target slug nor a target task
  element
- no `CanonicalRootError`, null-run placeholder, raw stack trace, or OAuth
  error appeared
- desktop `1440x1000` and genuine mobile `390x844` PASS

## Automated contracts

- Developer verification reported focused `141` OK and full regression `1369`
  OK with `5` skipped.
- QA automated contract:
  `python3 -m unittest tests.test_gbrain`: `267` passed.

## Live readback during documentation refresh

- `/api/health` and `/api/releases` read back `V0.0.166`.
- `/api/goal-execution` read back top-level `last_error: null`.
- `last_run.decisions` contained 13 decisions, including
  `goals/d837ac94-36f5-4735-93bb-d84c69b45435` with reason
  `owner_missing`.

## Operational boundary

This is a bounded hidden-item tolerance. It does not authorize ignoring missing
canonical roots, visible unsafe Agent-work issues, or malformed work that leaks
onto Board. Those cases remain fail-closed and require canonical data repair.

## Post-release fleet verifier HEAD pinning

Commit `8d4f31b458286ac9750b4b1e3a9f1b375189ff96`
(`Verify remote workers against local HEAD`) changed
`scripts/verify_handoff_worker_fleet.py` so an omitted `--expected-commit`
defaults to this checkout's local HEAD. This prevents each remote worker from
passing verification against its own stale repository HEAD.

Reported validation:

- focused verifier tests:
  `python3 -m unittest tests.test_handoff_worker_fleet_verifier tests.test_handoff_worker_runtime_verifier`
  → `7` OK
- remote Timmy host `toddy@100.100.126.85` fast-forwarded to
  `8d4f31b458286ac9750b4b1e3a9f1b375189ff96`
- Timmy LaunchAgent restarted and runtime verifier PASS
- fleet verifier reports Timmy OK against current HEAD
- Toddy remains `ssh_unreachable` at `toddy@100.117.212.20`

This is a verifier correctness improvement, not a Toddy recovery. Toddy must
not be documented as recovered and must not be worked around by installing a
local Toddy worker on this Mac.

## Post-release fleet verifier Tailscale diagnostics

Commit `6984f24c1fe330aca68fd95adc0a80dbcc9b4428`
(`Add Tailscale diagnostics to worker fleet verifier`) enriches SSH failures
with safe Tailscale peer metadata and diagnostic issues such as
`tailscale_key_expired` and `tailscale_peer_offline`.

Reported validation:

- focused verifier tests:
  `python3 -m unittest tests.test_handoff_worker_fleet_verifier tests.test_handoff_worker_runtime_verifier`
  → `8` OK
- remote Timmy host `toddy@100.100.126.85` fast-forwarded to
  `6984f24c1fe330aca68fd95adc0a80dbcc9b4428`
- Timmy runtime verifier PASS
- fleet verifier reports Timmy OK
- Toddy remains blocked with `ssh_unreachable` and `tailscale_key_expired`
- Toddy Tailscale peer metadata: name `Toddy's Mac Mini-1`, DNS
  `toddys-mac-mini-1.taildb46a7.ts.net.`, IP `100.117.212.20`

This is a diagnostic improvement, not a Toddy recovery. Toddy remains a remote
host/Tailscale remediation item, and this Mac remains Tammy/Tammy-OC local
worker only.
