# V0.0.61 release evidence

Date: 2026-08-01

Scope:

- Replacing or clearing a task's Next Action now preserves the previous value
  as a durable completed history entry.
- Task details render the current Next Action and completed history without
  inventing history for legacy tasks.
- Calendar Week keeps the selected task visible when details open and exposes
  the selected state to assistive technology in Week and Month.
- Calendar event reads do not run before local Calendar authorization.

Validation:

- Automated candidate checks: 251 tests passed; Python and JavaScript syntax
  checks and `git diff --check` passed.
- Dashboard-managed service `gtasks` restarted successfully and
  `http://127.0.0.1:4179/api/health` reported `V0.0.61`.
- Independent pre-commit UI/UX QA explicitly returned PASS at desktop
  1440 x 1000 and genuine mobile 390 x 844.
- Desktop Week retained seven usable columns with document width equal to the
  1440px viewport; Week and Month selected controls exposed
  `aria-current="true"` with readable selected styling.
- Settled mobile Saturday bounds were card x=30 through 242 and day x=21
  through 251 inside a 390px viewport/document.
- Fresh desktop and mobile sessions recorded zero console errors, failed
  requests, or pre-authorization `/api/ical-events` calls.
