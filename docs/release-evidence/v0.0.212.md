# V0.0.212 — Respect active handoff claims in Goal status

## Scope

- Product commit:
  `a48fe8e6abb5cb38e1dead64482bfd9871e2f6bb`.
- Goal execution treats a latest suppressed attention handoff row as
  in-flight/delivering when the latest delivery state has an active
  non-terminal execution claim.
- The active claim requires a recent `claimed_at`, future `expires_at`, and
  `terminal_state=null`.
- Suppressed handoffs without an active claim still render handoff repair
  attention.
- Stale retrying delivery still renders worker-unavailable attention.
- Local worker boundary is unchanged: local workers are Tammy/Tammy-OC only;
  Timmy/Toddy are remote/non-local.

## Independent QA

- Verdict: PASS.
- Frozen aggregate:
  `7471d32d38779d47ce11aa20aa9eca4eaa5e2de63f0d361f3e70aad82ba78d93`.
- QA report:
  `artifacts/qa/v0.0.212-independent/gate-report.md`.
- QA results:
  `artifacts/qa/v0.0.212-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About reported
  V0.0.212.
- Focused backend classification regressions passed, `3` OK:
  active suppressed handoff plus live claim remains delivering; suppressed
  terminal/no-claim remains `handoff_needs_repair`; stale queued delivery
  remains `handoff_worker_unavailable`.
- Desktop 1440x1000 and genuine mobile 390x844 verified the active-claim
  fixture rendered `Delivering` without `Needs attention` or system-review
  repair copy, while the no-claim and stale-retrying fixtures kept their
  attention states.
- Browser traffic was GET-only; the non-GET recorder captured zero writes and
  no live GBrain mutation occurred.

## Live readback

- Postdeploy `/api/goal-execution?refresh=1` returned
  `public_reason=actively_executing` for
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`.
- Summary readback: `total_goals=7`, `needs_attention=1`,
  `waiting_for_tony=0`, `in_flight=1`, `recently_completed=5`, and
  `last_error=null`.
- The remaining Family decision was an elapsed live `handoff_needs_repair`,
  not the active-claim fixture.

## Verification

- Developer full suite:
  `python3 -m unittest discover -s tests` = `1404` OK with `5` skipped.
- Developer focused coverage: `327` OK.
- Independent QA backend regression subset: `3` OK.
