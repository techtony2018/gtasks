# V0.0.186 — Link Goal execution action items

## Scope

- Agents > Goal execution summary action items are exact clickable controls:
  - waiting-for-Tony question text opens canonical Task detail;
  - missing-owner Goal title opens canonical Goal detail.
- Close restores focus to the exact originating summary control.
- Existing V0.0.185 summary data remains:
  - Entrepreneurship/default_agent_for `missing_owners`;
  - Family/Toddy `blocking_questions`.
- Navigation is read-only. It performs no GBrain, service, config, or source
  mutation.

## Canonical action items

- Waiting question Task: `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
- Missing-owner Goal: `goals/d837ac94-36f5-4735-93bb-d84c69b45435`
- Missing-owner relationship: `default_agent_for`
- Active public Goal execution task:
  `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`
- Postdeploy public reason: `actively_executing`

## Verification

- Focused frontend contract: OK.
- Full regression: `python3 -m unittest discover -s tests` — 1394 OK,
  5 skipped.

## QA

- Independent desktop and genuine mobile QA passed for the unchanged
  three-file product candidate with aggregate
  `c9a0bb8fd4f84ea45e6fd5c1e1214690f77ebe022f13a6accf435cb79d878f18`.
- QA report: `artifacts/qa/v0.0.186-independent/gate-report.md`.
- QA results: `artifacts/qa/v0.0.186-independent/gate-results.json`.
- Dashboard-managed `/api/health`, `/api/releases`, and About read back
  V0.0.186.
- Live `/api/goal-execution` top-level summary exactly matched
  `last_run.summary`; `last_error` was null.
- Postdeploy `/api/goal-execution` readback had:
  - `public_reason=actively_executing`;
  - selected task `tasks/08ca28c3-c812-5abf-86a7-110c14cb94a5`;
  - counts: `total_goals=7`, `needs_attention=2`,
    `waiting_for_tony=1`, `owner_missing=1`, `in_flight=1`,
    `recently_completed=3`;
  - `missing_owners[0]` for the Entrepreneurship Goal and
    `default_agent_for`;
  - `blocking_questions[0]` for task
    `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`.
- Desktop and mobile both verified the exact question control opened the
  canonical Task detail, focused the detail heading, and restored focus to the
  original summary control on Close.
- Desktop and mobile both verified the exact missing-owner Goal title control
  opened the canonical Goal detail, focused the detail heading, and restored
  focus to the original summary control on Close.
- QA observed zero console warnings/errors, zero failed requests, zero HTTP
  responses at or above 400, GET-only app API methods, and zero GBrain
  mutations.
- Landed commit:
  `84e624464d302f4f7cdc503f81c1e93dab22ec76`
  (`Link goal execution action items`).
