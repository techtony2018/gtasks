# Mission Control V0.0.160 release evidence

Date: 2026-08-24 (`America/Los_Angeles`)

Release: `V0.0.160`

Commit: `682c28e0b86a1f18357502bfd4e68f7777368536` (`Show Goal execution waiting on Tony`)

Runtime: dashboard-managed Mission Control at `http://127.0.0.1:4179`; `/api/health` read back `V0.0.160`.

QA and tests:

- Independent QA PASS:
  `artifacts/qa/v0.0.160-repair-independent/gate-report.md`
- Frozen QA aggregate:
  `0eefffc8cdae5ced352b50e8c2d74f47cde70d360a3ec1e911caee1979c4a836`
- Full suite: `python3 -m unittest discover -s tests` reported `1356` OK,
  `5` skipped.
- Static/runtime checks reported by release handoff:
  `node --check static/app.js` and
  `python3 -m compileall -q gtasks tests` passed.
- Independent QA focused suite reported `254` passed.

Verified behavior:

- Canonical Agent tasks with status `blocked`, canonical handoff state
  `waiting_for_input`, and `waiting_on=people/tony-guan` are classified by
  Goal execution as `waiting_for_tony` / Blocked, not
  `task_needs_next_action` / Needs attention.
- The UI copy is exactly:
  `The canonical task is blocked waiting for Tony's answer before the assigned Agent can continue.`
- Exact task detail hydrates the active handoff question TODO from canonical
  `todo_for` backlinks, so the handoff panel shows the real question instead
  of indefinite loading or No TODO.
- Bounded TODO hydration issues surface as a canonical TODO list unavailable
  error, shown as `The canonical TODO list is unavailable.`, while preserving
  task and handoff visibility.

Live operational readback:

- Task: `tasks/561640dd-8e34-43e1-a03e-e3f3f270033d`
- Task status: `blocked`
- Owner Agent: `agents/toddy`
- Canonical handoff state: `waiting_for_input`
- Handoff question TODO: `todos/99b64fec-aebe-57de-bf79-cc9d640a2db2`
- Blocker / waiting on: `people/tony-guan`
- Goal: `goals/2c86f86c-c9fb-5f49-96d0-e4d63f489fc8`
- Goal execution decision: `waiting_for_tony`

Operational boundary:

- This is a user-input blocked state. Do not document it as completed Agent
  execution, missing next action, missing handoff, or worker-route failure.
- A TODO hydration issue is readback/data-availability repair evidence. It
  does not authorize creating a replacement TODO, bypassing Tony's answer, or
  marking the task actionable.
