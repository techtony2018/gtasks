# Mission Control V0.0.145 release evidence

## Released baseline

- Version: `V0.0.145`
- Date: `2026-08-24` (`America/Los_Angeles`)
- Commit: `573d56cfb19f33f9d4bbc10140c315a9b6e6ba1d`
- Dashboard-managed health readback:
  `http://127.0.0.1:4179/api/health` returned `V0.0.145`.
- Developer release handoff reported:
  - `python3 -m py_compile gtasks/local_handoff_dispatcher.py`
  - focused new regression `1/1` OK
  - `python3 -m unittest tests.test_local_handoff_dispatcher tests.test_handoff_launch_runner tests.test_handoff_dispatcher` -> `222` OK
  - earlier full suite before commit `1337` OK, skipped `5`

## Verified behavior

V0.0.145 reconciles pre-gate terminal handoff recovery. Local Codex handoff
recovery now cancels an unused pre-gate launch and clears the wake inbox when
authoritative Mission Control recovery proves the same handoff is already
`completed` or `suppressed`.

Live readback for
`handoff-806a9277e5e24fda408872671512b27a4f9285494cfabc6f6de61a421dddac9f`
showed:

- local inbox state `completed`, `last_error=server_completed` per Developer
  handoff;
- server handoff status `completed`, reason `system_dependency_recovered`,
  target task `tasks/a6251324-1af6-5005-8a17-0ad0610be4d8`;
- handoff audit events ending with `acknowledgement` status `completed` and
  `execution_claim_released` status `completed`;
- target task `tasks/a6251324-1af6-5005-8a17-0ad0610be4d8` completed and linked
  to `collections/tammys-tasks`, `agents/tammy`, and Career Goal
  `goals/a405f27b-224a-52e5-ba0d-96c0e4202972`;
- Artifact `artifacts/32142bd1-8b1b-4ffc-a115-87fd39d7f6d7` titled
  `Career goal progress brief — 2026-08-24`, with typed
  `member_of -> collections/tammys-artifacts`, `created_by -> agents/tammy`,
  `produced_for -> tasks/a6251324-1af6-5005-8a17-0ad0610be4d8`, and
  `supports_goal -> goals/a405f27b-224a-52e5-ba0d-96c0e4202972`.

## Boundary

This release documents recovery behavior only. It does not change the standing
deployment policy: current behavior must be committed, pushed,
dashboard-deployed, and health/version read back before documentation calls it
current. It does not authorize documenting host-private token values,
credentials, raw prompts, private thread identifiers, or unrelated artifact
state.
