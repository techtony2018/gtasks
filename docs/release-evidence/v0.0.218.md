# V0.0.218 — Recover stale task refresh state and cheapen health

## Scope

- Product commit:
  `75964184124efc333d0df061bd2b616d88e479ee`.
- Task read-cache refreshes can supersede an expired background task read
  without leaving `/api/tasks` permanently refreshing or stale.
- `/api/health` reuses the bounded GBrain version readback instead of probing
  the runtime on every request.
- Dashboard-managed `/api/health` and `/api/releases` read back V0.0.218 with
  `gbrain 0.46.28.0`.

## Independent QA

- Verdict: PASS.
- Frozen aggregate:
  `90551eff928dd5496df6d5326f79de95dbdb63145ff5fcd175308d63d335932e`.
- QA report:
  `artifacts/qa/v0.0.218-independent/gate-report.md`.
- Dashboard-managed `/api/health` and `/api/releases` reported V0.0.218.
- Health exposed privacy-safe `gbrain 0.46.28.0`.
- Independent focused regression rerun passed: `101` tests in `3.594`
  seconds.

## Runtime readback

- Final receipt run: health cached p50 `1.95ms`.
- `/api/tasks` settled to fresh, `refreshing=false`, `stale=false`, 45 tasks,
  and zero issues by bounded poll 35.
- Five immediate `/api/tasks?refresh=1` reads stayed fresh with 45 tasks and
  zero issues.
- Independent QA observed initial `/api/tasks` and `/api/tasks?refresh=1`
  retaining 45 verified tasks and zero issues while background refresh was
  `refreshing/stale`; no false empty payload appeared.
- Final `/api/tasks` and `/api/tasks?refresh=1` were HTTP 200, fresh,
  non-stale, non-refreshing, 45 tasks, zero issues, and `error=null`.

## UI and mutation boundary

- Desktop 1440x1000 and mobile 390x844 kept Board and All Tasks populated with
  canonical task content.
- About showed `Mission Control V0.0.218 GBrain: gbrain 0.46.28.0`.
- Browser console had zero errors or warnings.
- Browser interactions were GET-only; non-GET count was zero.
- No source, service, configuration, git, or GBrain mutation occurred.
