# V0.0.76 release evidence

Status: `PRECOMMIT_REPAIR_QA_PASSED`

Source review: `APPROVED`. The reviewer verified all five earlier Important
findings repaired and returned no Critical or Important findings for candidate
fingerprint
`be4eee74ff413cd88194dcdff025a58c18e81e916b66a78c489c2bab9c4bb00e`.

The source review was repeated after the bounded live-canary repair. Independent
review returned `APPROVED` with no Critical or Important findings for repair
fingerprint
`fa762518de541d5ccf372ada95c2666fecc7dff50e3d94025608e2d578527aef`.

This file records source, QA, deployment, installation, and canary evidence for
Task 6. Canonical System Ticket completion is recorded on the ticket itself only
after its final page, relationship, and API readback.

## Candidate scope

- Event-driven handoffs are created only from verified canonical mutation
  receipts and exact post-write readback.
- One identity-scoped local Dispatcher resumes one pre-existing fixed Codex
  thread; no create, fork, replacement, or guessed-thread path is allowed.
- Task Timeline and Handoff Log are read-only views of the same redacted,
  append-only handoff audit events.
- Retry, terminal failure, capability rotation, crash recovery, bounded stale
  reconciliation, Guardian fallback, retention, export, and correction
  evidence remain attached to the same handoff identity and correlation.

## TDD and automated evidence

The focused Task 6 RED was observed before documentation or release metadata
was added:

```text
python3 -m unittest tests.test_releases tests.test_dashboard_integration -v
Ran 49 tests
FAILED (failures=7)
```

The failures were the missing V0.0.76 runtime/catalog entry, missing runbook,
missing release-evidence document, and sequential release expectation. The
dashboard runtime-path assertions passed. A separate pre-existing test drift
was corrected first: a linked-worktree-relative expectation was replaced by
the canonical runtime checkout `/Users/tony/work/gtasks`.

The original focused release/dashboard GREEN was:

```text
python3 -m unittest tests.test_releases tests.test_dashboard_integration -v
Ran 49 tests
OK
```

After the live-canary repair, the current focused repair suite and diff gate are
green:

```text
python3 -m unittest tests.test_local_handoff_dispatcher tests.test_releases
Ran 100 tests
OK
git diff --check
OK
```

The current root automated gate is green: 715 passed and 5 skipped. The final
UI candidate fingerprint is
`be4eee74ff413cd88194dcdff025a58c18e81e916b66a78c489c2bab9c4bb00e`.
The prior candidate fingerprints `b479...` and
`b5db8a71ef8e389df743db1536fdc91b9ccdac750cf65817da6d7c5cb06331d1`
were superseded during review repair.

## Independent pre-commit UI/UX QA

Independent QA returned explicit `PASS` on the unchanged final UI candidate
fingerprint above at desktop `1440x1000` and genuine mobile `390x844`.
It verified Task Timeline and Handoff Log same-source ordering and totals,
filters, correlation, pagination, keyboard/focus restoration, loading, empty,
stale, blocking-error, retry, dead-letter, lifecycle, long/redacted content,
44px mobile targets, narrow-screen containment, and visible V0.0.76 About and
release history. Recorded application traffic was GET-only; no live GBrain
mutation or Agent wake occurred. Browser, fixture, screenshots, and temporary
artifacts were cleaned up.

## Deployment and private identity readback

- GTasks source main and `origin/main` both read back
  `b9948bb2eeed467b980ca9e323a6444fb17de8e2`. The release merge is
  `576931018938db499862940d1adf3e1b421be28f`; the Tailnet proxy correction is
  `7008dbaf813ddef60d3da2a246b14c9a010bff23`.
- All Things Codex Dashboard source main was pushed at
  `47d744e5d03070085b7f023a28c262848e3ec9a2`. GTasks was restarted only through
  the dashboard-managed service API. Live PID `79653`, `/api/health`, the
  dashboard summary, and `/api/releases` all read back V0.0.76.
- Tailnet Serve exposes only `/api/handoffs/`; root and unrelated GTasks API
  paths return 404. Missing or invalid credentials return 401, and each valid
  host credential with the intentionally incomplete `{}` body returns the
  non-mutating `422 invalid_handoff_claim` contract.
- Canonical `agents/tammy`, `agents/timmy`, and `agents/toddy` pages read back
  three unique verified routes and SHA-256 registration references. The central
  credential file and handoff SQLite store use mode `0600`; no raw credential is
  projected into GBrain or audit UI.

## Three-host installation

Tammy, Timmy, and Toddy each run the canonical
`com.tony.gtasks-handoff-dispatcher` LaunchAgent with a verified compatible
Python, exact module checkout, existing fixed Agent workspace/thread, absolute
Codex path, mode-`0600` config/token files, and the single expected identity.
Both remote checkouts are clean at `b9948bb2eeed467b980ca9e323a6444fb17de8e2`.
The remote reinstall exposed a launchd unload/bootstrap timing race; the already
rendered canonical plists were bootstrapped directly and their loaded arguments,
working directories, and `PYTHONPATH` values were read back. All three runners
remain running.

## Tammy-only canary

The first malformed raw-registration fixture was dead-lettered with its audit
history preserved. A second hash-referenced attempt exposed two delivery
defects: non-Git Agent workspaces required `--skip-git-repo-check`, and local
nonzero/timeout retry failures were not paced. The independently reviewed
`b9948bb` repair added the exact flag contract and retry delay. A third synthetic
slug correctly returned `received` then `still_blocked` because no canonical
task existed; it was terminally dead-lettered without deleting evidence.

The final canary used the reserved, QA-owned canonical fixture
`tasks/70cf1aeb-ac30-4d78-995a-a1fea9d5bea9`, not Tony personal tasks or Agent
work. Correlation `corr-v0076-canary-4` contains exactly one Tammy handoff and
one attempt/generation with the chain `queued`, `leased`, `received`,
`actively_executing`, `completed`. The local claim file cleared after
completion. Timmy and Toddy had no claim files and no Codex resume child
processes; they were not canaried or woken.

## Post-deploy UI verification and repair

The first independent live post-deploy pass verified both required viewports,
the five-event canary chain, redaction, keyboard filtering, containment, V0.0.76,
zero console findings, and GET-only traffic, but returned `FAIL`: activating the
canary correlation could not open Task Timeline because the canonical QA fixture
was intentionally absent from Tony and Agent list projections.

Two RED regressions reproduced the boundary: exact QA-fixture read returned HTTP
404 and Handoff Log made no exact-task request. The uncommitted repair adds one
read-only exact canonical task endpoint and passes that verified task as the
existing detail fallback; it does not add QA fixtures to Tony personal or Agent
work lists. The two RED tests and the two pre-existing correlation regressions
are green.

Independent pre-commit UI/UX QA returned explicit `PASS` for repair fingerprint
`37bbcd1dc0f6407850024258a1bb554c9dcd0b4a3912a512b900f1dca86dd817`
at desktop `1440x1000` and genuine mobile `390x844`. It verified the fixture
remained absent from Tony/Agent lists, keyboard correlation activation issued
only the repaired exact GET, Task detail and the 5-of-5 Timeline opened with the
identical chain, close restored origin focus, raw correlation stayed redacted,
mobile remained exactly 390px with 44px controls, both consoles were clean, and
all application traffic was GET-only. The isolated fixture server and browser
artifacts were removed; the live dashboard-managed service was not restarted.

## Privacy and mutation boundary

Repository and audit output contain no bearer token, raw registration id, fixed
thread id, lease capability, full prompt, or private thread output. The only
canary task mutation was the explicitly QA-owned fixture in
`collections/mission-control-qa-fixtures`; Tony personal tasks and Agent work
were not mutated.

## Release gates

- Independent desktop `1440x1000` UI/UX QA: PASS
- Independent genuine mobile `390x844` UI/UX QA: PASS
- Commit and push: PASS
- Initial dashboard-managed deployment and V0.0.76 health readback: PASS
- Tammy, Timmy, and Toddy installations: PASS
- Tammy-only canary: PASS
- Independent pre-commit UI repair re-QA: PASS
- Final dashboard-managed repair deploy/readback: PENDING
- Canonical System Ticket evidence/completion: PENDING FINAL READBACK

Do not canary Timmy or Toddy in V0.0.76; neither was canaried in this release.
